from typing import List


class Solution:
    def permute(self, nums: List[int]) -> List[List[int]]:
        res = []
        def _permute(start, output):
            nonlocal res

        _permute(0, [])
        return res

sol = Solution()
print(sol.permute([1,2,3]))